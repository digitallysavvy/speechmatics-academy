# Ruff configuration for Speechmatics Academy
# Educational code should be clean but not pedantic

target-version = "py312"
line-length = 120

exclude = [
    # Ruff formats Python inside Markdown fenced blocks. README snippets in this
    # repo are deliberately partial and elided for teaching, so they are not
    # formatter input. Without this, `ruff format --check .` fails on 21
    # pre-existing READMEs and every PR goes red regardless of its contents.
    "*.md",
    "venv",
    ".venv",
    "node_modules",
    "__pycache__",
    "*.egg-info",
    ".git",
]

[lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes (unused imports, undefined names, etc.)
    "W",   # pycodestyle warnings
    "I",   # isort (import ordering)
]
ignore = [
    "E501",  # line length — handled by formatter, examples often have long descriptive strings
    "E402",  # module-import-not-at-top — examples use load_dotenv() / sys.path before local imports
]

[lint.isort]
known-first-party = ["speechmatics"]
